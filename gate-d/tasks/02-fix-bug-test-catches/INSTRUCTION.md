`test/parse.oracle.ts` fails. Fix the bug in `src/parse.ts` so every assertion passes —
`parsePort` must parse a base-10 non-negative integer string and throw on anything else.
Do not modify any file under `test/`.
