Test-first: in `test/clamp.test.ts`, write at least three assertions (using `node:assert`) for
`clamp(n, lo, hi)` — covering an in-range value, a value below `lo`, and a value above `hi`.
THEN implement `clamp` in `src/clamp.ts` so it clamps `n` into the inclusive range [lo, hi]
and your tests pass. Your implementation will also be checked against a hidden test suite.
