import type { Point } from "./geo.ts";

/**
 * Return the point in `candidates` closest to `p` (by Euclidean distance).
 * Must use `distance` from ./geo.ts (do not re-implement the distance math here).
 * (STUB — not implemented.)
 */
export function nearest(_p: Point, _candidates: Point[]): Point {
  throw new Error("notImplemented");
}
