"""Build the deterministic interval report."""
from collections.abc import Sequence


def build_report(value: object) -> dict[str, object]:
    """Validate intervals and return the normalized union and metrics."""
    raise NotImplementedError
